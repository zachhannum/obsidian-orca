---
title: Install
eyebrow: Start here
description: Put orca in a vault.
---

import Shot from '../../../components/Shot.astro';

Orca is a plugin for Obsidian on the desktop. It needs Obsidian 1.7.2 or later. Orca is not in the list of community plugins yet.

<Shot
  name="install"
  alt="The Community plugins tab of Obsidian's settings, with Orca in the list of installed plugins."
  caption="Settings, Community plugins. Orca is in the installed plugins."
  marks={['orca']}
/>

## Copy the plugin into the vault

A build of orca has four files: `main.js`, `manifest.json`, `styles.css` and `fleuron_bg.wasm`.

1. In the vault, make the folder `.obsidian/plugins/orca/`.
2. Copy the four files into that folder.
3. Restart Obsidian.

## Turn on orca

1. Open `Settings`.
2. Open `Community plugins`.
3. If community plugins are off, select `Turn on community plugins`.
4. Turn on `Orca`.

The ribbon then shows `Open Orca`, and the file tree offers `Create book from these notes` on a folder. [Make a book](/start/make-a-book/) is the next step.
